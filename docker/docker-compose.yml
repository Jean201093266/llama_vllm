version: "3.9"

services:
  inference-server:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: serve --config /app/configs/inference/server.yaml
    ports:
      - "8000:8000"
    volumes:
      - ../configs:/app/configs
      - ../outputs:/app/outputs

